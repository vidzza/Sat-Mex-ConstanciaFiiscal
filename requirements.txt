selenium>=4.18.0
webdriver-manager>=4.0.1
undetected-chromedriver>=3.5.5
Pillow>=10.0.0
# CAPTCHA solvers (instala al menos uno)
google-genai>=1.0.0         # GRATIS 1500 req/dia — aistudio.google.com (recomendado)
easyocr>=1.7.0              # OCR local sin API key — fallback automatico
ddddocr>=1.4.9              # OCR local alternativo
capsolver>=1.0.0            # opcional — pago, maxima precision
python-dotenv>=1.0.0
